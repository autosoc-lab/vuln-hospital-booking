services:
  web:
    build: .
    ports:
      - "${WEB_PORT:-5001}:5000"
    environment:
      APP_NAME: "Vulnerable Hospital Booking"
      SECRET_KEY: "dev-secret-key-change-me"
      FLASK_DEBUG: "1"
      PORT: "5000"
